#!/bin/bash

# Provision a VPC
# Create a `kubernetes` subnet
# Create a `kubernetes` firewall rules - internal and external
# Create a static public IP address for the external API access

# Create 3 compute instances within the subnet for Control plane
# Create worker instances within the Kubernetes Cluster CIDR range for Worker nodes

# Configure SSH access to the instances